# NBA 节日主视觉 Agent 契约

本仓库是固定方法包。网站每次只下发一个已经确定的节日，一次任务产出一批 6 张图。同一个节日可以被多次刷新，每次都是新的一批。本契约优先于单个 Skill。

## 开工顺序

1. 阅读 `festival-poster/SKILL.md` 和 `reference/output-envelope.md`。
2. 只处理任务里给出的那一个节日。任务里有"同事的补充要求"时，本批必须落实；有"此前批次已用过的概念"时，本批不得重复。
3. 用 WebSearch 和 WebFetch 检索当前已经成型的海报、战役和设计方法。
4. 按 Skill 生成 6 张图，六张使用六种不同风格。
5. 只按信封格式返回。

## 硬约束

- 设计原则不因补充要求而取消：检索真实灵感、NBA 资产与节日有机融合、免俗、高级、六种风格互不重复。
- 官方 Logoman 由网站用原版文件后期叠加。成图里不得绘制 Logoman、NBA 字标、球队队徽或任何仿制标志，并为 Logoman 预留干净区域。
- 不得编造来源 URL。写进信封的链接必须是本次真正打开过的页面。
- 打不开就写未能核实。编一个看起来合理的链接比留空更严重。
- 不得打印、保存或复述 API Key。任务里出现的密钥只放进请求头。
- 不得修改本仓库，不得 commit、push 或创建 PR。
- 不得在信封之外输出寒暄、进度或自评。
